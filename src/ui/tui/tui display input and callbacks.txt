Largely superseded by real code: TUI display/input now lives in `tui_render.c`/`_playarea.c`/`_io.c` and `tui_input.c` (this directory), orchestrated by `src/roles/stda/stda_tui.c`/`stda_tui_interactive.c`. Kept as a placeholder marker for the original module grouping, and because the event-callback half (vs. today's direct polling) is still unbuilt — see `ideas/2 engine and action system design/`. Original design intent was tracked in `ideas/3 tui/`; that folder's code prototype was fully superseded and deleted 2026-08-20, then renamed to `ideas/3 misc ui ideas/` since nothing TUI-specific remained there.
